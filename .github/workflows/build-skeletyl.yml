name: 'Build Example contra'
on:
  - workflow_dispatch
jobs:
  build:
    uses: ./.github/workflows/main.yml
    with:
      keyboard: '["skeletyl"]'
      alphas: '["dvorak"]'
      nav: '["vi"]'
      
