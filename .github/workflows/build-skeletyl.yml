name: 'Build Skeletyl'
on:
  - workflow_dispatch
jobs:
  build:
    uses: ./.github/workflows/main.yml
    with:
      keyboard: '["bastardkb/skeletyl"]'
      nav: '["vi"]'
      alphas: '["dvorak"]'
      
